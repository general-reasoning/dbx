name: dbx
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
    - -e .
    - pytest
    - ray[default]
    - fsspec
    - adlfs          # abfs://, abfss:// -- see datablocks[azure]
    - pandas
    - mosaicml-streaming
