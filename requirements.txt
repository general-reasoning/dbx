fsspec>=2023.1.0
gitpython>=3.1
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
pyyaml>=6.0
scikit-learn>=1.9.0
tqdm>=4.60
# Optional: torch>=2.0              (pip install datablocks[torch])
# Optional: ray>=2.0                (pip install datablocks[ray])
# Optional: mosaicml-streaming>=0.7 (pip install datablocks[streaming])
#           -- dbx.datastreams only; also needs torch
